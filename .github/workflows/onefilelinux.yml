

name: onefilelijnux

on:
  push:
    branches: 
      - master
  # schedule:
  #   - cron: 0 20 * * *
  # release:
  #   types: [published]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: onefile linux
        run: |
         systeminfo
         ipconfig /all
       
      - name: Upload uefi
        uses: actions/upload-artifact@master
        with:
          name: onefilelinux
          path: ./onefilelinux/


