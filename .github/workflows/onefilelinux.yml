name: onefilelinux

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
 - name: onefilelinux
        run: |
         chroot alpine-minirootfs /bin/ash
         ./build.sh
    
