

name: onefilelijnux

on:
  push:
    branches: 
      - master
  # schedule:
  #   - cron: 0 20 * * *
  # release:
  #   types: [published]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@master
        with:
          ref: master

      - name: onefile linux
        run: |
         echo wolegequ
       
      - name: Upload uefi
        uses: actions/upload-artifact@master
        with:
          name: onefilelinux
          path: ./onefilelinux/


